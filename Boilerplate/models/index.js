/**
 * @file index.js
 * @description Entry point for initializing Sequelize and defining all model associations.
 * 
 * TODO [Task 2]: Define the correct associations (HasMany, BelongsTo) for all models.
 * 
 * Requirements:
 * - Read config logic (usually loaded via Sequelize CLI).
 * - Initialize instances of User, Restaurant, Table, Reservation, etc.
 * - Establish relationships (e.g. User.hasMany(Reservation), Table.belongsTo(Restaurant)).
 * 
 * @see {@link ../Documentation/Tasks/2_Database_Models.md}
 */

// Implementation goes here

// Export your models and the sequelize instance
module.exports = {
    // User, Restaurant, etc...
};
