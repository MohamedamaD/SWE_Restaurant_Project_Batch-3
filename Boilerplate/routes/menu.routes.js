/**
 * @file menu.routes.js
 * @description Routes for Menu Categories and Items.
 * 
 * TODO [Task 6]: Define routes corresponding to menu.controller methods.
 * 
 * Note: Group into `/api/categories` and `/api/menu-items`.
 * Admin/Staff specific routes must be protected.
 * 
 * @see {@link ../Documentation/Tasks/6_Menu_Categories_Items.md}
 */

const express = require('express');
const router = express.Router();

module.exports = router;
